appName: "cloud-provider-aws"
repoName: "aws-cloud-controller-manager-app"
appCatalog: "giantswarm"

# CAPI providers to test against when triggering `/run app-test-suites` from a PR
providers:
  - capa
